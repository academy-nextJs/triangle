import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['media.istockphoto.com'], // دامنه‌ی مورد نظر را اینجا اضافه کنید
  },
  // سایر تنظیمات دیگر در اینجا
};

export default nextConfig;